version: 1.0
runtime: python311 # Use the latest supported Python version

build:
  commands:
    # Install dependencies (e.g., in a requirements.txt file)
    - pip install -r requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
