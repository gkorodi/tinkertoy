<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Information Form</title>
</head>
<body>

    <a href="/ui/form">Back to Form</a>

    <table border="1">
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Favorite Cloud Provider</th>
        </tr>
        {% for entry in entries %}
        <tr>
            <td>{{ entry.name }}</td>
            <td>{{ entry.email }}</td>
            <td>{{ entry.cloud }}</td>
            
        </tr>
        {% endfor %}
</body>
</html>